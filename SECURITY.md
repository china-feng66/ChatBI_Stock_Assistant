# Security

- 不提交 API Key、数据库密码、Cookie、访问令牌或可运行默认凭据。
- 数据库查询必须经过 `ReadOnlyQueryService`。
- 只允许单条 SELECT 或 SELECT-based CTE。
- SQLite 使用只读 URI，并启用 `query_only`。
- 查询时间和返回行数有上限。
- 测试使用合成数据库，不读取真实股票数据库。
- Agent 不直接执行模型生成的 Shell、文件路径或任意 SQL，只调用白名单确定性接口。
- 数据库路径只允许由服务端环境变量注入，API 请求不能指定本地路径。
- 所有策略与回测结论必须通过 Risk Critic，不能被动态权重跳过或降为零；只读行情快路径执行独立的数据证据门禁。
- Trace 不记录原始行情、完整问题、API Key 或数据库路径，只记录哈希、计数、证据 ID 和门禁结果。
- 默认 API 仅建议监听 `127.0.0.1`；公开部署前必须增加认证、限流和请求体限制。
- Redis、Chroma 和在线自动调权不属于当前安全基线。

如果凭据进入公开 Git 历史，应先在服务提供方撤销/轮换，再处理 Git 历史；仅修改当前文件不足以消除泄露。
